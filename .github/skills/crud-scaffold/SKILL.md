# CRUD Scaffold Skill

<!-- TODO (Día 3): Crear una skill para generar CRUDs end-to-end.
     Pasos: SQL table → EF Core model → DbContext → Controller + DTOs →
     Angular service → Component (list + detail) → Route.
     Reglas: máscaras en tokens, signals, standalone components. -->
